//------Problem Number-03-------//
function isLGSeven(number){
        const numSub = number -7;
        if(numSub < 7){
            return numSub;
        }
        else if(numSub >= 7){
            const result = number * 2;
            return result;
    }      
     }    
     //const result = isLGSeven(-15);
        //console.log(result);

//// comparison operators- returns true if the operands are equal and of the same type.
